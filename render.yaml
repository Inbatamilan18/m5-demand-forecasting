# Render.com Blueprint
# M5 Retail Demand Forecasting - FastAPI Backend

services:
  - type: web
    name: m5-demand-forecasting-api

    runtime: docker

    plan: free

    dockerfilePath: ./Dockerfile

    healthCheckPath: /health

    autoDeploy: true